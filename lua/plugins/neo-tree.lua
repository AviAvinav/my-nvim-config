-- won't use this for now but in case I decide to do later, I will just uncomment it

return {
  -- "nvim-neo-tree/neo-tree.nvim",
  -- branch = "v2.x",
  -- config = function()
  --   local neo_tree = require "neo-tree"
  --
  --   neo_tree.setup {
  --     filesystem = {
  --       filtered_items = {
  --         hide_dotfiles = false,
  --         hide_gitignored = false,
  --       },
  --     },
  --     window = {
  --       position = "right",
  --     },
  --   }
  -- end,
}
